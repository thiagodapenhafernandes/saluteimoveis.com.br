<!-- Imobill-Style Search Component (Tabs Outside) -->
<div class="w-full max-w-5xl mx-auto mt-4 md:mt-8 relative z-20" data-controller="search-tabs">

  <!-- Tabs (Comprar / Alugar) - Outside and Centered -->
  <div class="flex justify-center mb-3 md:mb-4">
    <div class="bg-white/90 backdrop-blur-sm p-1.5 rounded-full inline-flex shadow-lg border border-white/20">
      <button type="button"
              class="px-6 sm:px-8 py-2 rounded-full text-sm font-bold text-hero-button-text bg-hero-button shadow-sm transition-all active-tab"
              data-search-tabs-target="tab"
              data-action="click->search-tabs#switch"
              data-value="venda">
        Comprar
      </button>
      <button type="button"
              class="px-6 sm:px-8 py-2 rounded-full text-sm font-bold text-gray-600 hover:text-blue-three transition-all"
              data-search-tabs-target="tab"
              data-action="click->search-tabs#switch"
              data-value="aluguel">
        Alugar
      </button>
    </div>
  </div>

  <!-- Search Form Container -->
  <div class="bg-white rounded-2xl shadow-xl border border-gray-100">
    <%= form_with url: habitations_path, method: :get, class: "p-4 sm:p-6 lg:p-8" do |f| %>

      <input type="hidden" name="transaction_type" id="transaction_type_input" value="<%= params[:transaction_type] || 'venda' %>" data-search-tabs-target="input">
      <% selected_characteristics = Array(params[:characteristics]).map(&:to_s) %>
      <% price_range_options = [
        ["Todos os Valores", ""],
        ["até R$1.000.000", "0-1000000"],
        ["R$1.000.000 ↔ R$2.000.000", "1000000-2000000"],
        ["R$2.000.000 ↔ R$3.000.000", "2000000-3000000"],
        ["R$3.000.000 ↔ R$5.000.000", "3000000-5000000"],
        ["R$5.000.000 ↔ R$10.000.000", "5000000-10000000"],
        ["a partir de R$10.000.000", "10000000-"]
      ] %>

      <div class="grid grid-cols-1 md:grid-cols-12 gap-3 md:gap-4 items-end">
        
        <!-- Tipo de Imóvel (Checklist Dropdown) -->
        <div class="md:col-span-3" data-controller="category-filter">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Tipos de imóveis</label>
          <% selected_categories = Array(params[:category]).reject(&:blank?) %>
          <div class="relative">
            <button type="button"
                    class="w-full pl-10 pr-10 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium text-left"
                    data-category-filter-target="trigger"
                    data-action="click->category-filter#toggle">
              <i class="bi bi-building absolute left-3 top-3 text-gray-400"></i>
              <span data-category-filter-target="summary">Tipos de imóveis</span>
              <i class="bi bi-chevron-down absolute right-3 top-3 text-gray-400"></i>
            </button>

            <div class="hidden absolute top-full left-0 right-0 mt-1 bg-white border border-gray-200 rounded-xl shadow-xl z-50 max-h-[300px] overflow-y-auto overscroll-contain p-2"
                 style="-webkit-overflow-scrolling: touch;"
                 data-category-filter-target="panel">
              <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer border-b border-gray-100 mb-1">
                <input type="checkbox" class="form-check-input mt-0" data-category-filter-target="allCheckbox" data-action="change->category-filter#toggleAll">
                <span class="text-sm font-semibold text-gray-700">Todos</span>
              </label>

              <% (@property_types || []).each do |type| %>
                <% checked = selected_categories.include?(type) %>
                <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer">
                  <input type="checkbox"
                         value="<%= type %>"
                         class="form-check-input mt-0"
                         data-category-filter-target="checkbox"
                         data-action="change->category-filter#sync"
                         <%= "checked" if checked %>>
                  <span class="text-sm text-gray-700"><%= type %></span>
                </label>
              <% end %>
            </div>
          </div>

          <div data-category-filter-target="hiddenContainer">
            <% selected_categories.each do |category| %>
              <input type="hidden" name="category[]" value="<%= category %>">
            <% end %>
          </div>
        </div>
        
        <!-- Localização (Checklist Multiseleção) -->
        <div class="md:col-span-4" data-controller="location-filter">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Localização</label>
          <% selected_locations = Array(params[:city]).reject(&:blank?) %>
          <div class="relative">
            <button type="button"
                    class="w-full pl-10 pr-10 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium text-left"
                    data-location-filter-target="trigger"
                    data-action="click->location-filter#toggle">
              <i class="bi bi-geo-alt absolute left-3 top-3 text-gray-400"></i>
              <span data-location-filter-target="summary">Selecione bairros ou cidades</span>
              <i class="bi bi-chevron-down absolute right-3 top-3 text-gray-400"></i>
            </button>

            <div class="hidden absolute top-full left-0 right-0 mt-1 bg-white border border-gray-200 rounded-xl shadow-xl z-50 max-h-[320px] overflow-y-auto overscroll-contain p-2"
                 style="-webkit-overflow-scrolling: touch;"
                 data-location-filter-target="panel">
              <div class="sticky top-0 z-10 bg-white pb-2 space-y-2">
                <div class="flex items-center justify-between px-1 pt-1">
                  <span class="text-[11px] font-semibold uppercase tracking-wide text-gray-500">Localização</span>
                  <div class="flex items-center gap-2">
                    <span class="text-[11px] text-gray-500" data-location-filter-target="count">0 selecionados</span>
                    <button type="button"
                            class="text-[11px] font-semibold text-blue-three hover:underline"
                            data-action="click->location-filter#clear">
                      Limpar
                    </button>
                  </div>
                </div>
                <input type="text"
                       placeholder="Filtrar bairros/cidades"
                       class="w-full px-3 py-2 border border-gray-200 rounded-md text-sm focus:ring-1 focus:ring-blue-three focus:border-blue-three"
                       data-location-filter-target="search"
                       data-action="input->location-filter#filter">
              </div>

              <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer border-b border-gray-100 mb-1">
                <input type="checkbox" class="form-check-input mt-0" data-location-filter-target="allCheckbox" data-action="change->location-filter#toggleAll">
                <span class="text-sm font-semibold text-gray-700">Todos</span>
              </label>

              <% (@location_options || []).each do |option| %>
                <% checked = selected_locations.include?(option[:value]) %>
                <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer"
                       data-location-filter-row="true">
                  <input type="checkbox"
                         value="<%= option[:value] %>"
                         data-label="<%= option[:label].to_s.downcase %>"
                         class="form-check-input mt-0"
                         data-location-filter-target="checkbox"
                         data-action="change->location-filter#sync"
                         <%= "checked" if checked %>>
                  <span class="text-sm text-gray-700"><%= option[:label] %></span>
                </label>
              <% end %>
            </div>
          </div>

          <div data-location-filter-target="hiddenContainer">
            <% selected_locations.each do |city| %>
              <input type="hidden" name="city[]" value="<%= city %>">
            <% end %>
          </div>
        </div>
        
        <!-- Valores -->
        <div class="md:col-span-2">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Valores</label>
          <div class="relative">
            <i class="bi bi-chevron-down absolute right-3 top-3 text-gray-400 pointer-events-none"></i>
            <%= f.select :price_range,
                options_for_select(price_range_options, params[:price_range]),
                {},
                class: "w-full appearance-none px-4 pr-10 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium" %>
          </div>
        </div>

        <div class="md:col-span-3">
          <%= button_tag type: 'submit', name: nil, class: "w-full py-3 bg-hero-button hover:opacity-90 text-hero-button-text font-bold rounded-lg shadow-md hover:shadow-lg transition-all duration-200 flex items-center justify-center gap-2 uppercase tracking-wide text-sm" do %>
            <i class="bi bi-search"></i>
            <%= @home_setting.hero_cta_text.presence || "Buscar" %>
          <% end %>
        </div>
        
      </div>

      <div class="overflow-hidden max-h-0 opacity-0 pointer-events-none transition-all duration-500 ease-out"
           data-search-tabs-target="advancedPanel">
        <div class="mt-5 border-t border-gray-100 pt-5">
          <% metric_groups = [
            [:min_bedrooms, "Quartos", [["1", "1"], ["2", "2"], ["3", "3"], ["4+", "4"]]],
            [:min_suites, "Suítes", [["1+", "1"], ["2+", "2"], ["3+", "3"], ["4+", "4"]]],
            [:min_bathrooms, "Banheiros", [["1+", "1"], ["2+", "2"], ["3+", "3"], ["4+", "4"]]],
            [:min_parking, "Vagas", [["1+", "1"], ["2+", "2"], ["3+", "3"], ["4+", "4"]]]
          ] %>

          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            <% metric_groups.each do |field, label, options| %>
              <div>
                <div class="mb-1 text-xs font-bold uppercase tracking-wide text-gray-500"><%= label %></div>
                <div class="grid grid-cols-4 overflow-hidden rounded-lg border border-gray-200 bg-white">
                  <% options.each do |text, value| %>
                    <label class="cursor-pointer border-r border-gray-200 last:border-r-0">
                      <%= radio_button_tag field, value, params[field].to_s == value, class: "peer sr-only" %>
                      <span class="flex h-9 items-center justify-center text-xs font-semibold text-gray-600 transition-colors peer-checked:bg-blue-three peer-checked:text-white hover:bg-gray-50">
                        <%= text %>
                      </span>
                    </label>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>

          <div class="mt-5 grid grid-cols-1 gap-4 lg:grid-cols-2">
            <div>
              <div class="mb-2 text-xs font-bold uppercase tracking-wide text-gray-500">Buscas rápidas</div>
              <% quick_filters = [
                ['frente_mar', 'Frente Mar', 'bi-water'],
                ['quadra_mar', 'Quadra Mar', 'bi-building'],
                ['mobiliado', 'Mobiliado', 'bi-house-heart'],
                ['pronto', 'Pronto Morar', 'bi-check2-circle'],
                ['na_planta', 'Na Planta', 'bi-map'],
                ['opportunity', 'Ofertas', 'bi-tag-fill'],
                ['lancamento', 'Lançamento', 'bi-stars'],
                ['churrasqueira', 'Churrasqueira', 'bi-fire'],
                ['sacada', 'Sacada', 'bi-door-open']
              ] %>
              <div class="flex flex-wrap gap-2">
                <% quick_filters.each do |value, label, icon| %>
                  <label class="cursor-pointer">
                    <%= check_box_tag "characteristics[]", value, selected_characteristics.include?(value), class: "peer sr-only" %>
                    <span class="inline-flex h-9 items-center gap-2 rounded-full border border-gray-200 bg-white px-3 text-xs font-semibold text-gray-600 transition-all peer-checked:border-blue-three peer-checked:bg-blue-three peer-checked:text-white hover:border-blue-three hover:text-blue-three">
                      <i class="bi <%= icon %>"></i>
                      <%= label %>
                    </span>
                  </label>
                <% end %>

                <label class="cursor-pointer">
                  <%= check_box_tag :accepts_exchange, "1", params[:accepts_exchange].to_s == "1", class: "peer sr-only" %>
                  <span class="inline-flex h-9 items-center gap-2 rounded-full border border-gray-200 bg-white px-3 text-xs font-semibold text-gray-600 transition-all peer-checked:border-blue-three peer-checked:bg-blue-three peer-checked:text-white hover:border-blue-three hover:text-blue-three">
                    <i class="bi bi-arrow-left-right"></i>
                    Permuta
                  </span>
                </label>
              </div>
            </div>

            <div>
              <div class="mb-2 text-xs font-bold uppercase tracking-wide text-gray-500">Mais características</div>
              <% extra_filters = [
                ['cozinha_gourmet_churrasqueira', 'Cozinha gourmet com churrasqueira', 'bi-fire'],
                ['dependencia_empregada', 'Dependência', 'bi-door-closed'],
                ['sol_manha', 'Sol da manhã', 'bi-sunrise'],
                ['sol_tarde', 'Sol da tarde', 'bi-sunset'],
                ['sol_dia_todo', 'Sol o dia todo', 'bi-sun']
              ] %>
              <div class="flex flex-wrap gap-2">
                <% extra_filters.each do |value, label, icon| %>
                  <label class="cursor-pointer">
                    <%= check_box_tag "characteristics[]", value, selected_characteristics.include?(value), class: "peer sr-only" %>
                    <span class="inline-flex h-9 items-center gap-2 rounded-full border border-gray-200 bg-white px-3 text-xs font-semibold text-gray-600 transition-all peer-checked:border-golden-one peer-checked:bg-golden-one peer-checked:text-blue-three hover:border-golden-one hover:text-blue-three">
                      <i class="bi <%= icon %>"></i>
                      <%= label %>
                    </span>
                  </label>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-4 flex flex-wrap items-center justify-between gap-x-[0.2rem] gap-y-2 text-center text-sm font-bold text-blue-three">
        <button type="button"
                class="inline-flex items-center justify-center gap-1 text-blue-three transition-colors hover:text-golden-one"
                data-action="click->search-tabs#toggleAdvanced">
          <i class="bi bi-list" data-search-tabs-target="advancedIcon"></i>
          <span data-search-tabs-target="advancedLabel">Avançado</span>
        </button>

        <button type="button"
                onclick="document.dispatchEvent(new CustomEvent('open-code-search'))"
                class="inline-flex items-center justify-center gap-1 text-blue-three transition-colors hover:text-golden-one">
          <i class="bi bi-upc-scan"></i>
          Por código
        </button>

        <%= link_to empreendimentos_path, class: "inline-flex items-center justify-center gap-1 text-blue-three transition-colors hover:text-golden-one" do %>
          <i class="bi bi-buildings"></i>
          Empreendimentos
        <% end %>
      </div>
    <% end %>
  </div>
</div>
