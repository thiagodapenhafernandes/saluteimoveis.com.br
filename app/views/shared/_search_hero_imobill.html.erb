<!-- Imobill-Style Search Component (Tabs Outside) -->
<div class="w-full max-w-5xl mx-auto mt-8 relative z-20" data-controller="search-tabs">
  
  <!-- Tabs (Comprar / Alugar) - Outside and Centered -->
  <div class="flex justify-center mb-4">
    <div class="bg-white/90 backdrop-blur-sm p-1.5 rounded-full inline-flex shadow-lg border border-white/20">
      <button type="button" 
              class="px-8 py-2 rounded-full text-sm font-bold text-hero-button-text bg-hero-button shadow-sm transition-all active-tab"
              data-search-tabs-target="tab"
              data-action="click->search-tabs#switch"
              data-value="venda">
        Comprar
      </button>
      <button type="button" 
              class="px-8 py-2 rounded-full text-sm font-bold text-gray-600 hover:text-blue-three transition-all"
              data-search-tabs-target="tab"
              data-action="click->search-tabs#switch"
              data-value="aluguel">
        Alugar
      </button>
    </div>
  </div>

  <!-- Search Form Container -->
  <div class="bg-white rounded-2xl shadow-xl border border-gray-100">
    <%= form_with url: habitations_path, method: :get, class: "p-6 lg:p-8" do |f| %>
      
      <input type="hidden" name="transaction_type" id="transaction_type_input" value="<%= params[:transaction_type] || 'venda' %>" data-search-tabs-target="input">
      
      <div class="grid grid-cols-1 md:grid-cols-12 gap-4 items-end">
        
        <!-- Tipo de Imóvel (Checklist Dropdown) -->
        <div class="md:col-span-3" data-controller="category-filter">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Tipos de imóveis</label>
          <% selected_categories = Array(params[:category]).reject(&:blank?) %>
          <div class="relative">
            <button type="button"
                    class="w-full pl-10 pr-10 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium text-left"
                    data-category-filter-target="trigger"
                    data-action="click->category-filter#toggle">
              <i class="bi bi-building absolute left-3 top-3 text-gray-400"></i>
              <span data-category-filter-target="summary">Tipos de imóveis</span>
              <i class="bi bi-chevron-down absolute right-3 top-3 text-gray-400"></i>
            </button>

            <div class="hidden absolute top-full left-0 right-0 mt-1 bg-white border border-gray-200 rounded-xl shadow-xl z-50 max-h-[300px] overflow-y-auto overscroll-contain p-2"
                 style="-webkit-overflow-scrolling: touch;"
                 data-category-filter-target="panel">
              <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer border-b border-gray-100 mb-1">
                <input type="checkbox" class="form-check-input mt-0" data-category-filter-target="allCheckbox" data-action="change->category-filter#toggleAll">
                <span class="text-sm font-semibold text-gray-700">Todos</span>
              </label>

              <% (@property_types || []).each do |type| %>
                <% checked = selected_categories.include?(type) %>
                <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer">
                  <input type="checkbox"
                         value="<%= type %>"
                         class="form-check-input mt-0"
                         data-category-filter-target="checkbox"
                         data-action="change->category-filter#sync"
                         <%= "checked" if checked %>>
                  <span class="text-sm text-gray-700"><%= type %></span>
                </label>
              <% end %>
            </div>
          </div>

          <div data-category-filter-target="hiddenContainer">
            <% selected_categories.each do |category| %>
              <input type="hidden" name="category[]" value="<%= category %>">
            <% end %>
          </div>
        </div>
        
        <!-- Localização (Checklist Multiseleção) -->
        <div class="md:col-span-4" data-controller="location-filter">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Localização</label>
          <% selected_locations = Array(params[:city]).reject(&:blank?) %>
          <div class="relative">
            <button type="button"
                    class="w-full pl-10 pr-10 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium text-left"
                    data-location-filter-target="trigger"
                    data-action="click->location-filter#toggle">
              <i class="bi bi-geo-alt absolute left-3 top-3 text-gray-400"></i>
              <span data-location-filter-target="summary">Selecione bairros ou cidades</span>
              <i class="bi bi-chevron-down absolute right-3 top-3 text-gray-400"></i>
            </button>

            <div class="hidden absolute top-full left-0 right-0 mt-1 bg-white border border-gray-200 rounded-xl shadow-xl z-50 max-h-[320px] overflow-y-auto overscroll-contain p-2"
                 style="-webkit-overflow-scrolling: touch;"
                 data-location-filter-target="panel">
              <div class="sticky top-0 z-10 bg-white pb-2 space-y-2">
                <div class="flex items-center justify-between px-1 pt-1">
                  <span class="text-[11px] font-semibold uppercase tracking-wide text-gray-500">Localização</span>
                  <div class="flex items-center gap-2">
                    <span class="text-[11px] text-gray-500" data-location-filter-target="count">0 selecionados</span>
                    <button type="button"
                            class="text-[11px] font-semibold text-blue-three hover:underline"
                            data-action="click->location-filter#clear">
                      Limpar
                    </button>
                  </div>
                </div>
                <input type="text"
                       placeholder="Filtrar bairros/cidades"
                       class="w-full px-3 py-2 border border-gray-200 rounded-md text-sm focus:ring-1 focus:ring-blue-three focus:border-blue-three"
                       data-location-filter-target="search"
                       data-action="input->location-filter#filter">
              </div>

              <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer border-b border-gray-100 mb-1">
                <input type="checkbox" class="form-check-input mt-0" data-location-filter-target="allCheckbox" data-action="change->location-filter#toggleAll">
                <span class="text-sm font-semibold text-gray-700">Todos</span>
              </label>

              <% (@location_options || []).each do |option| %>
                <% checked = selected_locations.include?(option[:value]) %>
                <label class="flex items-center gap-2 px-2 py-2 rounded-md hover:bg-gray-50 cursor-pointer"
                       data-location-filter-row="true">
                  <input type="checkbox"
                         value="<%= option[:value] %>"
                         data-label="<%= option[:label].to_s.downcase %>"
                         class="form-check-input mt-0"
                         data-location-filter-target="checkbox"
                         data-action="change->location-filter#sync"
                         <%= "checked" if checked %>>
                  <span class="text-sm text-gray-700"><%= option[:label] %></span>
                </label>
              <% end %>
            </div>
          </div>

          <div data-location-filter-target="hiddenContainer">
            <% selected_locations.each do |city| %>
              <input type="hidden" name="city[]" value="<%= city %>">
            <% end %>
          </div>
        </div>
        
        <!-- Valor Aproximado (Target Price) -->
        <div class="md:col-span-2">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Valor Aproximado</label>
          <div class="relative">
            <span class="absolute left-3 top-3 text-gray-400 z-10 text-sm font-bold pt-0.5">R$</span>
            <%= f.text_field :target_price, 
                value: params[:target_price],
                placeholder: "Ex: 1.000.000",
                data: { controller: "currency-mask", action: "input->currency-mask#format" },
                class: "w-full pl-10 pr-4 py-3 bg-white border border-gray-200 rounded-lg focus:ring-1 focus:ring-blue-three focus:border-blue-three text-gray-700 text-sm font-medium placeholder-gray-400" %>
          </div>
        </div>

        <!-- Código (Botão Modal) -->
        <div class="md:col-span-1">
          <label class="block text-xs font-bold text-gray-500 uppercase mb-1 ml-1">Código</label>
          <button type="button" 
                  onclick="document.dispatchEvent(new CustomEvent('open-code-search'))"
                  class="w-full px-2 py-3 bg-white border border-gray-200 rounded-lg hover:border-blue-three hover:text-blue-three text-gray-500 text-sm font-medium transition-colors flex items-center justify-center gap-1"
                  title="Buscar por código">
            <i class="bi bi-house-check"></i>
            <span class="hidden md:inline">Cód</span>
          </button>
        </div>
        
        <div class="md:col-span-2">
          <%= button_tag type: 'submit', name: nil, class: "w-full py-3 bg-hero-button hover:opacity-90 text-hero-button-text font-bold rounded-lg shadow-md hover:shadow-lg transition-all duration-200 flex items-center justify-center gap-2 uppercase tracking-wide text-sm" do %>
            <i class="bi bi-search"></i>
            <%= @home_setting.hero_cta_text.presence || "Buscar" %>
          <% end %>
        </div>
        
      </div>
      
    <% end %>
  </div>
</div>
